1. model
2. /guestbook/deleteform/10 pathvariable!!
    (/guestbook/deleteform?no=10)
    1) urls
        python_ch3 > python_ch3 > urls

        path('helloworld/hello2/<int:id>', helloworld_views.hello), ++

3. orm method
    index(list) -> ok
    add         -> ok
    delete
    1. repository 에서 객체를 영속화(sync? insert 할때 .save() // )
        guestbook = Guestbook.objects.filter(id=20).filter(ppassword='1234')
        guestbook.delete()


[구현 안해도 되는 것]
list.html 템플릿에서
 1. 메세지 index 잡는 것 하지말 것 (django template arith operation)
 2. 메세지 개행문자 <br> replace 하지말 것

